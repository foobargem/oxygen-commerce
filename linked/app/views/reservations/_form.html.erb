  <%= f.error_messages %>
    쿠폰: <%= Coupon.find(session[:user_id]).coupon_number %>
    사용자명: <%= Coupon.find(session[:user_id]).purchaser_name %><p>
    이용일 선택: <% CalendarDateSelect.format=(:iso_date)%><%= f.calendar_date_select :used_at %></br>
    <%= f.input :resort, :as => :string, :label => "스키장 선택" %></br>
    <%= f.input :part_time, :as => :string, :label => "시간 선택" %></br>
    <%= f.input :height, :as => :string, :label => "신장" %></br>
    <%= f.input :shoe_size, :as => :string, :label => "발사이즈" %></br>
