<ul class="subscriber_nav">
  <%= navigation_tag :li, current_controller?("reservations") && current_action?("index"),
   (link_to "예약목록", reservations_path) %>
  <%= navigation_tag :li, current_controller?("reservations") && current_action?("new"),
   (link_to "예약하기", new_reservation_path) %>
</ul>
