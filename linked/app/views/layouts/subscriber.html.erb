<!DOCTYPE HTML>
<html>
<head>
  <meta charset="UTF-8">
  <meta http-equiv="content-type" content="text/html; charset=utf-8" />
  <meta name="Author" content="OxygenComputing Inc." />
  <title>투어포스코리아::예약시스템</title>
  <%= stylesheet_link_tag "standard", "suddenlybunt", "static", "publisher" %>
  <%= javascript_include_tag :defaults %>
  <%= (yield :stylesheets) if content_for?(:stylesheets) %>
  <%= (yield :javascripts) if content_for?(:javascripts) %>
  <%= csrf_meta_tag %>
</head>
<%= body_START %>

  <div id="h">
    <h1>
      <%= link_to "투어포스코리아 예약시스템", root_path %>
    </h1>

    <ul class="account_nav">
      <% if logged_in? %>
        <li class="name"><em><%= current_user.purchaser_name %></em> 님</li>
        <li class="spacer">|</li>
        <li><%= link_to "logout", logout_path %></li>
      <% else %>
        <li><%= link_to "login", login_path %></li>
      <% end %>
    </ul>
  </div>

  <div id="contents">
    <%= yield %>
  </div>

  <div id="footer">
  </div>

<%= body_END %>
</html>
